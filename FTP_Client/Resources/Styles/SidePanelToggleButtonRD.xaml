<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:FTP_Client.Resources.Controls"
                    xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
                    xmlns:sys="clr-namespace:System;assembly=mscorlib">

    <Style x:Key="SidePanelToggleButtonStyle" TargetType="ToggleButton">
        <Setter Property="Background" Value="{StaticResource PanelBackgroundBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
        <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
        <Setter Property="Margin" Value="5,5" />
        <Setter Property="Width" Value="30" />
        <Setter Property="Height" Value="30" />
        <Setter Property="Padding" Value="5" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="1,1,0,1">

                        <Path Fill="{TemplateBinding Foreground}"
                              Width="18"
                              Height="18" 
                              Stretch="Uniform"
                              HorizontalAlignment="Center" 
                              VerticalAlignment="Center"
                              Data="M 19.42,13.62 C 19.34,13.11 19.22,12.63 19.05,12.18 L 20.2,11.03 C 20.59,10.64 20.59,10 20.2,9.61 L 17.39,6.799 C 17,-6.41 16.36,-6.41 15.97,6.799 L 14.82,7.949 C 14.37,7.779 13.89,7.659 13.38,7.579 L 13.13,6.289 C 13.04,5.719 12.55,5.279 12,5.279 L 9,5.279 C 8.45,5.279 7.96,5.719 7.87,6.289 L 7.62,7.579 C 7.11,7.659 6.63,7.779 6.18,7.949 L 5.03,6.799 C 4.64,-6.41 4,-6.41 3.61,6.799 L 0.799,9.61 C 0.409,10 0.409,10.64 0.799,11.03 L 1.95,12.18 C 1.78,12.63 1.66,13.11 1.58,13.62 L 0.289,13.87 C 0.289,13.87 0.289,13.87 0.289,13.87 C -0.291,13.96 -0.291,14.04 -0.291,15 C -0.291,15.55 -0.291,16.04 0.289,16.13 L 1.58,16.38 C 1.66,16.89 1.78,17.37 1.95,17.82 L 0.799,18.97 C 0.409,19.36 0.409,20 0.799,20.39 L 3.61,23.2 C 4,23.59 4.64,23.59 5.03,23.2 L 6.18,22.05 C 6.63,22.22 7.11,22.34 7.62,22.42 L 7.87,23.71 C 7.96,24.28 8.45,24.72 9,24.72 L 12,24.72 C 12.55,24.72 13.04,24.28 13.13,23.71 L 13.38,22.42 C 13.89,22.34 14.37,22.22 14.82,22.05 L 15.97,23.2 C 16.36,23.59 17,23.59 17.39,23.2 L 20.2,20.39 C 20.59,20 20.59,19.36 20.2,18.97 L 19.05,17.82 C 19.22,17.37 19.34,16.89 19.42,16.38 L 20.71,16.13 C 21.29,16.04 21.29,15.55 21.29,15 C 21.29,14.04 21.29,13.96 20.71,13.87 C 20.71,13.87 20.71,13.87 20.71,13.87 Z M 10.5,18 C 8.57,18 7,16.43 7,14.5 C 7,12.57 8.57,11 10.5,11 C 12.43,11 14,12.57 14,14.5 C 14,16.43 12.43,18 10.5,18 Z" />

                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource AccentHoverBrush}" />
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource AccentBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="SidePanelCloseButtonStyle" TargetType="Button" >
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
        <Setter Property="Width" Value="24" />
        <Setter Property="Height" Value="24" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="3">
                        <Path Data="M 0 0 L 10 10 M 10 0 L 0 10"
                              Stroke="{TemplateBinding Foreground}"
                              StrokeThickness="1.5"
                              Width="10" 
                              Height="10"
                              Stretch="Uniform"
                              HorizontalAlignment="Center" 
                              VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource ErrorBrush}" />
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type local:SidePanelControl}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:SidePanelControl">
                    <Grid x:Name="LayoutRoot"
                          Background="Transparent">

                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="ExpansionStates">
                                <VisualState x:Name="Collapsed">
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="ContentBorder" 
                                                         Storyboard.TargetProperty="Width" 
                                                         To="0" 
                                                         Duration="0:0:0.2" />
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="Expanded">
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="ContentBorder" 
                                                         Storyboard.TargetProperty="Width" 
                                                         To="250" 
                                                         Duration="0:0:0.2" />
                                    </Storyboard>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>

                        <b:Interaction.Triggers>
                            <b:DataTrigger Binding="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}}" Value="True">
                                <b:GoToStateAction StateName="Expanded" 
                                                   TargetName="LayoutRoot" />
                            </b:DataTrigger>
                            <b:DataTrigger Binding="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}}" Value="False">
                                <b:GoToStateAction StateName="Collapsed" 
                                                   TargetName="LayoutRoot" />
                            </b:DataTrigger>
                        </b:Interaction.Triggers>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>

                            <ToggleButton Grid.Column="0"
                                          FontSize="14"
                                          Style="{StaticResource SidePanelToggleButtonStyle}"
                                          Content="{TemplateBinding Header}"
                                          ToolTip="{TemplateBinding Header}"
                                          IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"/>

                            <Border x:Name="ContentBorder"
                                    Grid.Column="1"
                                    Grid.RowSpan="2"
                                    Width="0"
                                    ClipToBounds="True"
                                    Background="{StaticResource WindowBackgroundBrush}"
                                    BorderBrush="{StaticResource BorderBrush}"
                                    BorderThickness="0,1,1,1">

                                <DockPanel>
                                    <Button DockPanel.Dock="Top"
                                            Style="{StaticResource SidePanelCloseButtonStyle}"
                                            HorizontalAlignment="Right"
                                            Margin="4">
                                        <b:Interaction.Triggers>
                                            <b:EventTrigger EventName="Click">
                                                <b:ChangePropertyAction TargetObject="{Binding RelativeSource={RelativeSource TemplatedParent}}"
                                                                        PropertyName="IsExpanded"
                                                                        Value="False" />
                                            </b:EventTrigger>
                                        </b:Interaction.Triggers>
                                    </Button>

                                    <ContentPresenter Content="{TemplateBinding Content}" />

                                </DockPanel>
                            </Border>
                        </Grid>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>